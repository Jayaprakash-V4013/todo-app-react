# Todo App (React)

A simple Todo application built with React and JavaScript.

## Features
- Add / Edit / Delete todos
- Filters: All, Active, Completed
- Due dates
- LocalStorage persistence

## Setup
1. Install dependencies: `npm install`
2. Run the app: `npm run dev`